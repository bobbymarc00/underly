# Evidence Policy

- Keep raw probe JSON/CSV outside the public repo if it contains wallet identifiers you do not want public.
- Never store API keys or secrets.
- Public evidence should include timestamps, endpoint names, non-sensitive request parameters, Binance response codes and derived calculations.
- Underly Proof v1 hashes the API response payload generated by the engine; it is not an on-chain attestation.
